# The current dependency-free browser prototype uses no environment variables.
# Public World Labs embed URLs are configured in worlds.json.
# No private World Labs or TRIPO key is exposed to the browser.
